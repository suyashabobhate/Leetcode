class Solution {
    public int countSegments(String s) {
        int count=0;
        String []b=s.split(" ");
        for(int i=0;i<b.length;i++){
            if(b[i].length()!=0){
                count++;
            }
        }
        return count;
    }
}
