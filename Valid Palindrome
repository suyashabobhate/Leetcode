class Solution {
    public boolean isPalindrome(String s) {
         s = s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
	StringBuffer g=new StringBuffer(s);
	String b=g.reverse().toString();
	if(b.equals(s))
		return true;
	else return false;
    }
}
